name: mnist-sweep
# program: -m mains.train_mnist
project: linear-ae-2541
method: grid
command:
  - cd "/h/huang/git_code/lae/linear-ae" ; ${env}
  - ${interpreter} -m
  - mains.train_mnist
  - ${args}
parameters:
  optimizer:
    distribution: categorical
    values:
      - SGD
      - Adam
  model_name:
    distribution: categorical
    values:
      - uniform_sum
      - non_uniform_sum
      - rotation
      - nd
      - nd_exp
      - vae
